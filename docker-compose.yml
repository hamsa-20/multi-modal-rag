version: "3.8"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./:/workspace
    environment:
      - PYTHONUNBUFFERED=1
    command: streamlit run src/ui/app.py --server.port 8501 --server.address 0.0.0.0
